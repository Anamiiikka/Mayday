# The Mayday backend (backend/ — npm run dev)
API_URL=http://localhost:4000

# Must match backend/.env. The Command Room sends this from the server when it
# dispatches an agent or clears an action; it is never exposed to the browser.
# Generate with: openssl rand -hex 24
OPERATOR_TOKEN=
